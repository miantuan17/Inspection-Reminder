name: Workday Reminder
on:
  schedule:
    - cron: '30 07 * * *'   # 👉 北京时间 15:30 精准对应
  workflow_dispatch:

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run script
        env:
          WECHAT_URL: ${{ secrets.WECHAT_URL }}
          FEISHU_URL: ${{ secrets.FEISHU_URL }}
        run: python3 remind.py
